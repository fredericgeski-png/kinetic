## Packages
@hookform/resolvers | For connecting Zod schemas to react-hook-form
recharts | For dashboard analytics charts and data visualization
date-fns | Human-readable date formatting

## Notes
Tailwind Config:
No changes needed, the CSS variables for `--font-sans` and `--font-mono` will be updated in `index.css` to use the 'Geist' and 'Geist Mono' fonts for a sleek, technical appearance.
Dark mode is forced at the `:root` level for a consistent "professional dev tool" aesthetic.
